/**
 * Reserva/liberação de seriais e caixas — lógica HTTP em preparacaoStock.js e stockRemaining.js.
 * Extrair funções puras aqui em PRs futuros se necessário.
 */
const { STOCK_STATUS } = require('./loteStatus');

module.exports = {
  STOCK_STATUS,
};
