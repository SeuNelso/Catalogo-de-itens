/**
 * Fase 6: rotas de armazéns/transferências em `server/index.js` migrarão para `server/routes/catalog/`.
 * Ver `index.js` nesta pasta. As rotas continuam montadas em `index.js` até migração incremental.
 */
module.exports = {
  /** Reservado para futuro `createArmazensRouter(deps)` quando as rotas forem cortadas do monólito. */
};
