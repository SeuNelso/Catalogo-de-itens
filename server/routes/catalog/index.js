/**
 * Rotas de catálogo / armazéns extraídas de `server/index.js` (Fase 6).
 * `armazensStub.js` mantém-se como ponto de entrada legado; novos routers exportam-se aqui.
 */
module.exports = {
  // createArmazensRouter: require('./armazens') — migrar GET /api/armazens incrementalmente
};
