# Configuração do Banco de Dados PostgreSQL
# Use DATABASE_URL para conexão completa ou configure individualmente
DB_HOST=localhost
DB_PORT=5432
DB_NAME=catalogo
DB_USER=seu_usuario
DB_PASSWORD=sua_senha
DATABASE_URL=postgres://usuario:senha@localhost:5432/catalogo

# Copiar dados do Railway para o banco local (npm run db:pull-railway)
# Copie do Railway: projeto → PostgreSQL → Connect → Variable DATABASE_URL
DATABASE_URL_RAILWAY=

# Configuração do Servidor
PORT=3001
NODE_ENV=development

# JWT Secret (GERE UMA CHAVE SEGURA!)
# Use: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=sua-chave-secreta-aqui-altere-em-producao

# Cloudflare R2 (Armazenamento de Imagens)
# Obtenha essas credenciais no painel do Cloudflare R2
R2_BUCKET=seu-bucket-name
R2_ENDPOINT=https://seu-endpoint.r2.cloudflarestorage.com
R2_ACCESS_KEY=sua-access-key
R2_SECRET_KEY=sua-secret-key

# Google Drive (Opcional - para armazenamento alternativo)
GOOGLE_DRIVE_FOLDER_ID=

# Google Cloud Vision API (Opcional - para reconhecimento de imagens)
GOOGLE_APPLICATION_CREDENTIALS=./credentials.json
